import React from 'react';
import Styled from 'styled-components';

const NavDiv = Styled.div`
    display: flex;
`;

const Nav = () => {
	return (
		<NavDiv>
			<p>Hi!</p>
		</NavDiv>
	);
};

export default Nav;
